# 04 · HTTP Server (no framework)

The built-in `http` module — the foundation Express sits on.

```bash
npx tsx examples/04-http-server/index.ts
curl http://localhost:4000/health
```

See [docs/03-http-and-rest-apis.md](../../docs/03-http-and-rest-apis.md).
