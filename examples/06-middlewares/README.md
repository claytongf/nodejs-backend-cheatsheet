# 06 · Middlewares

Global logging middleware + a per-route guard. Order matters.

```bash
npx tsx examples/06-middlewares/index.ts
curl -H "x-api-key: secret" http://localhost:4002/secret
```

See [docs/04-express-fundamentals.md](../../docs/04-express-fundamentals.md).
