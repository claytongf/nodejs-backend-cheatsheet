# 10 · Tests

A minimal Jest unit test. (Supertest/HTTP integration tests live in `src/tests/`.)

```bash
npx jest examples/10-tests
```

> The main `jest.config.js` scopes test discovery to `src/`, so run this example by
> pointing Jest at the folder explicitly.

See [docs/12-testing-jest-supertest.md](../../docs/12-testing-jest-supertest.md).
