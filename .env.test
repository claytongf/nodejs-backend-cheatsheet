# Test environment configuration (safe, local-only values — committed on purpose).
# Tests run against a SEPARATE database so they never touch your dev data.
NODE_ENV=test
LOG_LEVEL=error
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/taskmanager_test?schema=public
JWT_SECRET=test-secret-not-for-production
JWT_EXPIRES_IN=1h
